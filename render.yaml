services:
  - type: web
    name: pre-repeter-backend
    runtime: python
    buildCommand: |
      pip install poetry
      poetry install
    startCommand: poetry run python main.py  # or your actual entrypoint
